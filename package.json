{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My Portfolio Website",
  "main": "index.js",
  "scripts": {
  "build": "npx rimraf dist && mkdir dist && cp -r public/* dist/ && cp index.js dist/"
},

  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
