{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My portfolio site",
  "main": "index.js",
  "scripts": {
  "build": "rimraf dist && mkdir dist && xcopy /E /I public\\* dist\\ && copy index.js dist\\"
},

  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

